REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
REACT_APP_GOOGLE_DRIVE_API_KEY=
REACT_APP_GOOGLE_DRIVE_UPLOAD_ENDPOINT=
REACT_APP_SENTRY_DSN=
REACT_APP_PUBLIC_APP_URL=

# Push notifications (Web Push VAPID)
# Generate keys: node scripts/generate-vapid-keys.js
# Private key lives in Supabase project secrets. Never expose it client-side.
# Public key is safe for the browser.
REACT_APP_VAPID_PUBLIC_KEY=

# Email (Resend/SendGrid)
# Provider API keys live in Supabase project secrets, for example RESEND_API_KEY
# or SENDGRID_API_KEY. Never expose email provider secrets through REACT_APP_*.
# Also set EMAIL_FROM in Supabase project secrets, for example:
# BLW York Hub <no-reply@your-domain.com>
# If you add signed unsubscribe token validation, keep UNSUBSCRIBE_SECRET
# in Supabase project secrets only.

# Zoom
# Zoom credentials live in Supabase project secrets:
# ZOOM_ACCOUNT_ID, ZOOM_CLIENT_ID, ZOOM_CLIENT_SECRET
